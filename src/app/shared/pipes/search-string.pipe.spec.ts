import { SearchStringPipe } from './search-string.pipe';

describe('SearchStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStringPipe();
    expect(pipe).toBeTruthy();
  });
});
